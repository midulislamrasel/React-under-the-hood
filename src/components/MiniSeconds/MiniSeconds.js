import React from 'react';

const MiniSeconds = (props) => {
    return (
        <div>
            <p>Mini Seconds {props.state}</p>
        </div>
    );
};

export default MiniSeconds;<p>Mini Seconds {}</p>